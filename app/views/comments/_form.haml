= form_with model: [@article, @comment] do |f|
  = f.label :content
  %br
  = f.text_area :content 
  - if @comment.errors.any?
    - @comment.errors.full_messages.each do |error|
      %p= error

  %br

  = f.submit


