%h1 Hello %World! 
%p This is coming from articles#index

%table
  %thead
  %tr
    %th Title
    %th Author
    %th Content
    %th Action
  %tbody
  - @articles.each do |article|
    %tr 
      %td= article.title
      %td= article.author
      %td= article.content.truncate(100)
      %td
        = link_to 'Show', article_path(article.id)
        |
        = link_to 'Edit', edit_article_path(article.id)  
        | 
        = link_to 'Delete', article_path(article.id), method: :delete, data: {confirm: "Are you sure"} 


%br
  = link_to 'New Article', 'articles/new'
                    